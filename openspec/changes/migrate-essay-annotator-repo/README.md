# migrate-essay-annotator-repo

Move Essay Annotator from the legacy ProjectVault repo container into a standalone GitHub-linked project while preserving local runtime data and the frontend-base to main release workflow.
